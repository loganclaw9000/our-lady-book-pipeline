# Scribe Handoff — 2026-04-24

**For:** future Scribe instance post-context-compact
**Role:** you are SCRIBE (novel-worker for our-lady-book-pipeline). Counterpart = FORGE (voice-FT maintainer at /home/admin/paul-thinkpiece-pipeline/). Both fully autonomous, no human arbiter. Operator = Paul.

## Live state snapshot (as of 14:59Z 2026-04-24)

**Milestone hit this session:** Chapter 1 shipped end-to-end.
- 3 scenes committed (Andrés Havana / Malintzin Potonchán / Itzcoatl Tenochtitlan) — all em-dash 0
- Chapter DAG complete: 4 atomic commits (canon + entity-state + rag-reindex + retrospective)
- canon/chapter_01.md = 2852 words
- chapter_sha `74452ddecb761ebde78d6dda1b61df98ae57220f`
- All critic axes >=3 (historical 72, metaphysics 90, entity 80+, arc 78, donts 85+)

**Infra live:**
- vLLM 0.17.0 serving paul-voice on `127.0.0.1:8003` (V6 Qwen3.5-27B-LoRA bnb 4-bit, 0.55 GPU util, 75Gi RSS stable)
- voice_pin manifest_digest `83095d9371f26f7979c85b1ba47f05dd21cd1f677023c98c891b0fab11c72ca1` (matches Forge MANIFEST bit-for-bit)
- claude-code CLI backend for critic + regenerator + entity extractor + retrospective writer (OAuth subscription, zero marginal cost)
- LanceDB indexes regenerated with new 9-field CHUNK_SCHEMA (includes source_chapter_sha from Plan 05-03)

**Branch state:** origin/main HEAD = `187116c fix(chapter): CLI-side EncoderShim adapter`. All work pushed.

## V7C training window (LOCKED)

Forge hands off GPU at **T-0 = 2026-04-25 17:00 UTC** (10:00 PT). T-5min kill of vLLM at 16:55Z. V7C SFT runs 10h, merged + new MANIFEST at ~03:00Z 2026-04-26. Scribe then re-pins voice + re-boots vllm against new LoRA.

**Expected Forge messages between now and then:**
- T-60min warning at **2026-04-25T16:00Z** (≈ when this handoff is ~1h old)
- T-0 kill confirmation at 16:55Z
- V7C ship msg ~03:00Z next day w/ new manifest_digest

## On wake — first actions

1. `date -u +"%H:%MZ"` — check current time vs Forge schedule
2. `wc -l /home/admin/paul-thinkpiece-pipeline/_coordination/voice_to_novel.jsonl` — detect new Forge msgs
3. `tail -n +77 /home/admin/paul-thinkpiece-pipeline/_coordination/voice_to_novel.jsonl | grep -v heartbeat` — read non-hb msgs (line 76 was last seen; adjust if new)
4. `curl -s --max-time 3 http://127.0.0.1:8003/v1/models` — verify vLLM alive
5. Post a heartbeat to `/home/admin/paul-thinkpiece-pipeline/_coordination/novel_to_voice.jsonl` using protocol below

## Protocol (PROTOCOL.md v2.1 + 9 invariants)

**Dead drop:** `/home/admin/paul-thinkpiece-pipeline/_coordination/`
- `voice_to_novel.jsonl` (Forge → Scribe) — append-only
- `novel_to_voice.jsonl` (Scribe → Forge) — append-only
- `incident_log.jsonl` + `PROTOCOL.md` + `voice_pipeline_state.json`

**Tiers:**
- T1_active: 60s heartbeats (active collab)
- T2_both_idle: 270s (cache-warm)
- T3_long_idle: 1200s (after 3 idle cycles)
- T0_HEADS_DOWN: no heartbeats during multi-edit cycles, only state-change msgs
- Auto-escalate T2/T3 → T1 on any non-heartbeat msg

**Heartbeat JSON shape:**
```json
{"timestamp":"<UTC ISO>","from":"scribe","type":"heartbeat","subject":"scribe heartbeat #<N>","body":"{\"wakeup_count\":<N>,\"gpu_state\":\"serving_paul_voice_idle\",\"last_action\":\"<short>\",\"next_eta\":\"<when/what>\",\"inbox_unread_count\":0,\"tier\":\"T3_long_idle\"}","priority":"low","correlation_id":"forge_<last_hb_ts>"}
```

**Message types:** ship, request, warning, feedback, incident, ack, heartbeat, proposal

## Hard constraints

- Claude Code CLI backend (default) — NOT Anthropic SDK — for critic/regen/extractor/retro/Mode-B drafter. Factory at `src/book_pipeline/llm_clients/factory.py::build_llm_client`. Default kind `claude_code_cli`.
- vLLM gpu_memory_utilization max 0.60 (Forge safety ceiling, wedge risk)
- Forge owns model decisions (V7C timing, anchor drift policy). Scribe owns book content.
- Anchor centroid stays paul-original-only even post-V7C (anchor_drift_policy_v1 canonical).

## Decisions log (canonical from Forge dead-drop decisions.jsonl):

- `voice_samples_contract_v1`: rich samples-with-metadata shape (not flat passages list). Scribe coerces via Pydantic validator.
- `anchor_drift_policy_v1`: paul-original anchors stay canonical even post-V7C.
- `dry_run_gate_v1.1`: SHA verify before jinja render at dry-run time (catches digest drift pre-boot).
- `t0_heads_down_v1`: no heartbeats during multi-edit cycles.
- `v7c_window_a`: 2026-04-25T17:00Z start, 10h exclusive.
- `rag_reingest_slot_v1`: option (b) — re-ingest AFTER V7C merge (5-min gap before vllm re-boot).

## What's NOT done (backlog)

- ch02+ scene authoring (manual yaml stubs; outline-parser CLI not yet wired into CH flow)
- `.planning/pipeline_state.json` (untracked, appears after each chapter DAG — gitignore candidate)
- novel_worker_feedback.jsonl mirror per chapter commit — 1 row posted for ch01, future chapters should auto-mirror per Phase 4 RetrospectiveWriter extension (not yet built)
- 4 stale background Monitor tasks — not cleaned up, don't matter (all succeeded or timed out)

## Key files (absolute paths)

**Committed artifacts:**
- `canon/chapter_01.md` — the chapter
- `retrospectives/chapter_01.md` — Opus post-mortem
- `entity-state/chapter_01_entities.json` — extractor output
- `indexes/{entity_state,arc_position}.lance/` — fresh 9-field schema
- `drafts/ch01/ch01_sc0{1,2,3}.md` — scene buffer (drafts/ tracked)
- `runs/smoke/ch01_sc0{1,2,3}_*.md` — frozen smoke samples

**Source code you might touch:**
- `src/book_pipeline/cli/chapter.py` — composition root for chapter DAG; has `_EncoderShim` adapter (don't move into kernel — Paul reverted prior attempts)
- `src/book_pipeline/cli/draft.py` — scene loop composition root
- `src/book_pipeline/drafter/mode_a.py` + `mode_b.py` + `regenerator/scene_local.py` — all 3 have `_apply_forge_postprocess*` wraps
- `config/voice_pin.yaml` — V6 27B pin
- `config/voice_samples.yaml` → symlink to Forge's voice_samples_v6.yaml

**Reference:**
- `runs/incidents/INCIDENT_2026-04-24_first_vllm_boot.md` — vllm venv_cu130 vs /usr/bin/python3 lesson
- `HANDOFF_VOICE_MODEL.md` in paul-thinkpiece-pipeline — Forge's operator doc

## Memory pointers (in `~/.claude/projects/-home-admin-Source-our-lady-book-pipeline/memory/`)

- `feedback_no_vllm_during_build.md` — historical, partially superseded (Forge coordinates now)
- `reference_vllm_serving.md` — vllm lives in /usr/bin/python3

## Last emitted outbound msg

HB12 at 2026-04-24T09:04:25Z — "ch01 shipped, parking T3". Forge acked line 71 "CH01 DAG COMPLETE — huge milestone acked". Last Forge line = 76 (heartbeats only since ack).

## Wake cadence pre-V7C

Currently scheduled every ~1h until ~14:59Z. At 15:59Z (1h before T-60min warning), drop to T1 60s in case Forge escalates early. At 16:00Z expect Forge T-60min warning msg → T1 active comms begin.

## Emergency contacts

- Paul = operator. In slash-command session as you read this.
- Forge = sibling Claude in `/home/admin/paul-thinkpiece-pipeline/`. Use dead-drop only.
- If wedge: `pkill -9 -f vllm` immediately, then `free -h` should jump back to >100Gi available.
