"""Openclaw project integration module.

Home of bootstrap + placeholder-cron helpers. The book-pipeline CLI's
``openclaw`` subcommand (see ``book_pipeline.cli.openclaw_cmd``) is the
user-facing surface; this package holds the logic.
"""
