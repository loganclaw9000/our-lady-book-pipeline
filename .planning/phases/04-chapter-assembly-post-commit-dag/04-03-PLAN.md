---
phase: 04-chapter-assembly-post-commit-dag
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/book_pipeline/entity_extractor/opus.py
  - src/book_pipeline/entity_extractor/schema.py
  - src/book_pipeline/entity_extractor/templates/extractor_system.j2
  - src/book_pipeline/entity_extractor/__init__.py
  - src/book_pipeline/retrospective/opus.py
  - src/book_pipeline/retrospective/lint.py
  - src/book_pipeline/retrospective/templates/retrospective_system.j2
  - src/book_pipeline/retrospective/__init__.py
  - tests/entity_extractor/test_opus.py
  - tests/entity_extractor/test_schema.py
  - tests/retrospective/test_opus.py
  - tests/retrospective/test_lint.py
autonomous: true
requirements: [CORPUS-02, TEST-01]

must_haves:
  truths:
    - "OpusEntityExtractor satisfies the frozen EntityExtractor Protocol and writes `entity-state/chapter_{NN:02d}_entities.json` with EntityCards each carrying `source_chapter_sha` mandatory (V-3)."
    - "EntityExtractor does incremental update: reads all existing `entity-state/chapter_*.json`, passes consolidated prior-card view to Opus, emits new+updated cards only."
    - "OpusRetrospectiveWriter satisfies the frozen RetrospectiveWriter Protocol and writes `retrospectives/chapter_{NN:02d}.md`."
    - "Retrospective output passes a lint rule: must reference ≥1 scene_id (regex `ch\\d+_sc\\d+`) AND ≥1 critic-issue artifact (axis name OR chunk_id OR evidence quote)."
    - "Both components use `build_llm_client(critic_backend_cfg)` and emit exactly ONE Event per invocation (role='entity_extractor' OR 'retrospective_writer')."
  artifacts:
    - path: "src/book_pipeline/entity_extractor/opus.py"
      provides: "OpusEntityExtractor class — Protocol-conformant + incremental + source_chapter_sha stamped"
      contains: "class OpusEntityExtractor"
    - path: "src/book_pipeline/entity_extractor/schema.py"
      provides: "EntityExtractionResponse Pydantic model — claude --json-schema contract"
      contains: "class EntityExtractionResponse"
    - path: "src/book_pipeline/retrospective/opus.py"
      provides: "OpusRetrospectiveWriter class — Protocol-conformant + Claude Code CLI backend + lint-enforced output"
      contains: "class OpusRetrospectiveWriter"
    - path: "src/book_pipeline/retrospective/lint.py"
      provides: "lint_retrospective() function — rejects generic boilerplate per success criterion 5"
      contains: "def lint_retrospective"
  key_links:
    - from: "src/book_pipeline/entity_extractor/opus.py"
      to: "src/book_pipeline/llm_clients/factory.py"
      via: "build_llm_client(critic_backend_cfg) — subscription-covered OAuth subprocess backend"
      pattern: "build_llm_client"
    - from: "src/book_pipeline/entity_extractor/schema.py"
      to: "src/book_pipeline/interfaces/types.py"
      via: "EntityExtractionResponse wraps list[EntityCard] (frozen)"
      pattern: "entities: list[EntityCard]"
    - from: "src/book_pipeline/retrospective/opus.py"
      to: "src/book_pipeline/retrospective/lint.py"
      via: "writer invokes lint_retrospective on output; retry-once on lint fail with nudge"
      pattern: "lint_retrospective"
---

<objective>
Land two post-commit DAG writers that run in parallel in Wave 2:

1. **OpusEntityExtractor (CORPUS-02)** — reads committed `canon/chapter_{NN}.md`, calls Claude Code CLI with `--json-schema EntityExtractionResponse` for structured output, filters against a consolidated view of prior-chapter entity cards (incremental update: only NEW or changed entities emitted), writes result to `entity-state/chapter_{NN:02d}_entities.json`. Every EntityCard stamps `source_chapter_sha = <canon commit sha>` for stale-card detection (CORPUS-02 success criterion 6).

2. **OpusRetrospectiveWriter (TEST-01 retrospective side)** — reads the committed chapter, slices `runs/events.jsonl` by `caller_context.chapter`, plus scene critic reports + voice-fidelity scores + per-scene attempt_count, and generates a markdown retrospective with sections "What Worked", "What Drifted", "Emerging Patterns", "Open Questions for Next Chapter" + YAML frontmatter carrying `candidate_theses`. Output MUST pass the lint rule (reference ≥1 scene_id AND ≥1 critic-issue artifact) or the writer re-invokes Opus ONCE with a "cite {sid}/{axis}" nudge; second lint fail logs warning + commits anyway (ungated: soft signal per CONTEXT.md).

Purpose: CORPUS-02 stands up the entity-card auto-generation that enables Phase 4+ entity_state RAG retrievers to see cross-chapter continuity. TEST-01 stands up the retrospective artifact that Phase 6 thesis matcher consumes.

Both components share the same LLM backend (`build_llm_client(critic_backend_cfg)`) as the chapter critic (Plan 04-02). Plans 04-02 + 04-03 have disjoint `files_modified` and run in parallel in Wave 2.

Output:
- `OpusEntityExtractor` satisfies `isinstance(e, EntityExtractor)` runtime check.
- `OpusRetrospectiveWriter` satisfies `isinstance(w, RetrospectiveWriter)` runtime check.
- `EntityExtractionResponse` Pydantic schema (for `claude --json-schema`).
- `lint_retrospective()` pure function (reusable by tests + Plan 04-04 DAG orchestrator).
- 2 system prompt templates (extractor + retrospective).
- Tests: 8+ for EntityExtractor (schema conformance, incremental diff, sha stamping, Event emission, tenacity retry 3×, prior-cards deserialization, mocked CLI E2E, missing-chapter handling) + 6+ for RetrospectiveWriter (protocol, lint happy path, lint fail+retry+success, lint fail twice+warn+commit, Event emission, markdown structure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-chapter-assembly-post-commit-dag/04-CONTEXT.md
@.planning/phases/04-chapter-assembly-post-commit-dag/04-01-PLAN.md
@src/book_pipeline/interfaces/entity_extractor.py
@src/book_pipeline/interfaces/retrospective_writer.py
@src/book_pipeline/interfaces/types.py
@src/book_pipeline/critic/scene.py
@src/book_pipeline/llm_clients/claude_code.py
@src/book_pipeline/llm_clients/factory.py
@src/book_pipeline/observability/__init__.py
@.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-09-GAP-SUMMARY.md

<interfaces>
<!-- Frozen contracts. -->

From src/book_pipeline/interfaces/entity_extractor.py (FROZEN):
```python
@runtime_checkable
class EntityExtractor(Protocol):
    def extract(
        self,
        chapter_text: str,
        chapter_num: int,
        chapter_sha: str,
        prior_cards: list[EntityCard],
    ) -> list[EntityCard]: ...
```
Post-condition: every returned EntityCard.source_chapter_sha == chapter_sha. EventLogger.emit(Event(role='entity_extractor', ...)) called before return.

From src/book_pipeline/interfaces/retrospective_writer.py (FROZEN):
```python
@runtime_checkable
class RetrospectiveWriter(Protocol):
    def write(
        self,
        chapter_text: str,
        chapter_events: list[Event],
        prior_retros: list[Retrospective],
    ) -> Retrospective: ...
```

From src/book_pipeline/interfaces/types.py (FROZEN):
```python
class EntityCard(BaseModel):
    entity_name: str
    last_seen_chapter: int
    state: dict[str, object] = Field(default_factory=dict)  # {aliases, entity_type, first_mentioned_chapter, current_state, relationships, confidence_score}
    evidence_spans: list[str] = Field(default_factory=list)
    source_chapter_sha: str  # mandatory per CORPUS-02

class Retrospective(BaseModel):
    chapter_num: int
    what_worked: str
    what_didnt: str
    pattern: str
    candidate_theses: list[dict[str, object]] = Field(default_factory=list)
```

From src/book_pipeline/llm_clients/claude_code.py (structured output path):
```python
client.messages.parse(
    model="claude-opus-4-7",
    messages=[{"role": "user", "content": <prompt>}],
    output_format=EntityExtractionResponse,  # pydantic BaseModel
    system=[{"type": "text", "text": <system>, "cache_control": {...}}],
    max_tokens=4096,
    temperature=0.1,
)
# → ParseResponse(parsed_output: EntityExtractionResponse, usage: _Usage(...), model=...)
```

From src/book_pipeline/llm_clients/claude_code.py (text output path):
```python
client.messages.create(
    model="claude-opus-4-7",
    messages=[{"role": "user", "content": <prompt>}],
    system=[{"type": "text", "text": <system>, "cache_control": {...}}],
    max_tokens=4096,
)
# → CreateResponse(content=[_TextBlock(text=<markdown>)], usage=...)
```

From Plan 04-02 (pattern mirror for structured-output critic):
- Pre-render system prompt once.
- _system_blocks list object reused.
- tenacity retry on (APIConnectionError, APIStatusError), N attempts, wait_exponential, reraise=True.
- _handle_failure writes audit-like record + error Event BEFORE raise.
- `from book_pipeline.llm_clients import build_llm_client`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpusEntityExtractor (CORPUS-02) + EntityExtractionResponse schema + incremental-update diff + tests</name>
  <read_first>
    - src/book_pipeline/interfaces/entity_extractor.py (Protocol)
    - src/book_pipeline/interfaces/types.py — EntityCard shape (source_chapter_sha mandatory, state dict carrying aliases/entity_type/first_mentioned_chapter/current_state/relationships/confidence_score per CONTEXT.md)
    - src/book_pipeline/critic/scene.py (tenacity decorator pattern + Event emission pattern to mirror)
    - src/book_pipeline/llm_clients/claude_code.py (ParseResponse shape; parse() expects `output_format: type[BaseModel]`)
    - .planning/phases/04-chapter-assembly-post-commit-dag/04-CONTEXT.md § "EntityExtractor (CORPUS-02, grey-area c)"
  </read_first>
  <behavior>
    - `from book_pipeline.entity_extractor.opus import OpusEntityExtractor; from book_pipeline.entity_extractor.schema import EntityExtractionResponse`.
    - `e = OpusEntityExtractor(anthropic_client=..., event_logger=..., model_id="claude-opus-4-7"); isinstance(e, EntityExtractor) is True`.
    - `e.extract(chapter_text, chapter_num=1, chapter_sha="deadbeef123", prior_cards=[])` returns `list[EntityCard]` where every card has `source_chapter_sha == "deadbeef123"`.
    - `EntityExtractionResponse` Pydantic model: `{entities: list[EntityCard], chapter_num: int, extraction_timestamp: str}` — exactly as per CONTEXT.md grey-area c.
    - Incremental update logic:
      1. When `prior_cards` is non-empty, pass `prior_entity_summary` in the user prompt as a compact JSON list of `{entity_name, last_seen_chapter, current_state_summary}` (don't dump full state dict).
      2. Receive full entity list from Opus in response.
      3. Diff against prior: an entity is NEW if its `entity_name` not in `{p.entity_name for p in prior_cards}`; UPDATED if name matches AND any state field differs from the prior card's latest state.
      4. Return ONLY new or updated cards. Unchanged prior entities ARE NOT re-returned (idempotency guarantee).
      5. Test: prior_cards=[Cortés(state.current_state="in Havana")]; Opus returns [Cortés(current_state="in Veracruz"), Motecuhzoma(first_mentioned_chapter=1)]; `extract()` returns 2 cards (updated Cortés + new Motecuhzoma).
    - Tenacity retry: 3 attempts (NOT 5 — this is per CONTEXT.md: "tenacity 3× retry on transient"), `wait_exponential(multiplier=2, min=2, max=30)`, `retry_if_exception_type((APIConnectionError, APIStatusError))`, `reraise=True`.
    - Persistent failure after 3 attempts → raise `EntityExtractorBlocked(reason='entity_extraction_failed', chapter_num, cause)`. The Plan 04-04 DAG orchestrator maps this to `ChapterState.DAG_BLOCKED`.
    - One `role="entity_extractor"` Event per invocation (success XOR error). `caller_context = {module, function, chapter_num, chapter_sha, entity_count, new_cards, updated_cards, prior_cards_count}`. Error Event carries `extra.status = "error"` + `extra.error = <reason>`.
    - `source_chapter_sha` stamping: post-process the parsed entities — override `card.source_chapter_sha = chapter_sha` on every entity (defense-in-depth; the prompt also instructs Opus to pass it through).
    - Missing or empty chapter_text raises `EntityExtractorBlocked(reason='empty_chapter', chapter_num)` BEFORE calling Opus.
  </behavior>
  <action>
    1. Create `src/book_pipeline/entity_extractor/schema.py` (~40 LOC):
       ```python
       """Claude --json-schema contract for entity extraction (CORPUS-02)."""
       from __future__ import annotations
       from pydantic import BaseModel, Field
       from book_pipeline.interfaces.types import EntityCard


       class EntityExtractionResponse(BaseModel):
           """Top-level response schema for OpusEntityExtractor.

           claude --json-schema validates the entire payload shape at the CLI
           boundary; we re-validate post-receive via model_validate().
           """
           entities: list[EntityCard] = Field(default_factory=list)
           chapter_num: int
           extraction_timestamp: str  # ISO-8601
       ```
    2. Create `src/book_pipeline/entity_extractor/templates/extractor_system.j2`:
       - System prompt: "You extract entity cards (people, places, objects, events) from a committed chapter of Our Lady of Champion."
       - Instructions: "For each named entity, emit an EntityCard with: entity_name (canonical), last_seen_chapter=<chapter_num>, state dict with keys {aliases: list[str], entity_type: one of [person, place, object, event], first_mentioned_chapter: int, current_state: short natural-language summary ≤100 words, relationships: list[{to: <entity>, kind: <verb>}], confidence_score: 0..1}, evidence_spans (quoted strings from chapter_text, ≤3 per entity), source_chapter_sha (pass through the value provided)."
       - "If prior entity cards are provided in the user message, ONLY emit cards for entities whose state has CHANGED since prior, or NEW entities not in prior. Do not re-emit unchanged prior entities."
       - Output format block: "Return ONLY JSON matching the EntityExtractionResponse schema."
    3. Create `src/book_pipeline/entity_extractor/opus.py` (~400 LOC mirroring Plan 04-02 ChapterCritic structure):
       - Module docstring citing CORPUS-02 + incremental-update contract.
       - `DEFAULT_EXTRACTOR_SYSTEM = Path("src/book_pipeline/entity_extractor/templates/extractor_system.j2")`.
       - `_TENACITY_MAX_ATTEMPTS = 3` (per CONTEXT.md; deviates from 5 used in critic+regen — log this in Task 1 summary).
       - `EntityExtractorBlocked(Exception)` with `reason` + `**context`.
       - `OpusEntityExtractor` class:
         - `__init__(self, *, anthropic_client, event_logger, model_id="claude-opus-4-7", max_tokens=4096, temperature=0.1, template_path=DEFAULT_EXTRACTOR_SYSTEM)`. Pre-render system prompt from template (Jinja2 FileSystemLoader, StrictUndefined). Store `self._system_blocks = [{"type": "text", "text": self._system_prompt, "cache_control": {"type": "ephemeral", "ttl": "1h"}}]`.
         - `extract(self, chapter_text, chapter_num, chapter_sha, prior_cards) -> list[EntityCard]`:
           1. Validate `chapter_text.strip()` non-empty → else raise EntityExtractorBlocked('empty_chapter', chapter_num=chapter_num).
           2. Build user prompt: chapter_text (full) + prior-cards compact JSON summary + explicit `source_chapter_sha="{chapter_sha}"` + `chapter_num={chapter_num}` + ISO timestamp.
           3. `_call_opus_inner(messages=[{"role": "user", "content": user_prompt}])` — tenacity-wrapped, 3 attempts, messages.parse with output_format=EntityExtractionResponse.
           4. Failure path → `_handle_failure(...)` emits error Event BEFORE raise EntityExtractorBlocked.
           5. Post-process: override `card.source_chapter_sha = chapter_sha` on EVERY entity (defense-in-depth).
           6. Diff against prior_cards: build `prior_name_to_card = {c.entity_name: c for c in prior_cards}`; for each extracted card, include if (a) name not in prior_name_to_card OR (b) name in prior_name_to_card AND `extracted.state != prior.state`.
           7. Count `new_cards = sum(1 for c in returned if c.entity_name not in prior_name_to_card)`; `updated_cards = len(returned) - new_cards`.
           8. Emit Event(role="entity_extractor", caller_context={module, function, chapter_num, chapter_sha, entity_count=len(returned), new_cards, updated_cards, prior_cards_count=len(prior_cards)}).
           9. Return the filtered list.
         - `@tenacity.retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=2, min=2, max=30), reraise=True) def _call_opus_inner(self, *, messages): …` — retry_if_exception_type on (APIConnectionError, APIStatusError).
         - `_handle_failure(...)` — emit error Event with `extra = {"status": "error", "error_type": type(exc).__name__, "error_message": str(exc), "attempts_made": 3}` BEFORE caller raises.
    4. Update `src/book_pipeline/entity_extractor/__init__.py`: `from book_pipeline.entity_extractor.opus import OpusEntityExtractor, EntityExtractorBlocked; from book_pipeline.entity_extractor.schema import EntityExtractionResponse` + `__all__`.
    5. Create `tests/entity_extractor/test_schema.py` — 2 tests:
       - `test_entity_extraction_response_validates` — build valid + invalid payloads through `EntityExtractionResponse.model_validate(...)`.
       - `test_schema_json_schema_is_stable` — `EntityExtractionResponse.model_json_schema()` contains keys {entities, chapter_num, extraction_timestamp}; snapshot-assertion so the CLI contract doesn't drift silently.
    6. Create `tests/entity_extractor/test_opus.py` — 8 tests (use `FakeAnthropicClient` pattern from `tests/critic/fixtures.py`, extended to return `ParseResponse(parsed_output=EntityExtractionResponse(...))`):
       - `test_A_protocol_conformance` — `isinstance(OpusEntityExtractor(anthropic_client=..., event_logger=...), EntityExtractor) is True`.
       - `test_B_source_chapter_sha_stamped` — Fake returns entities with arbitrary `source_chapter_sha`; extractor is called with `chapter_sha="abc123"`; assert every returned card has `source_chapter_sha == "abc123"` (override applied).
       - `test_C_incremental_filters_unchanged` — prior_cards=[Cortés(state={"current_state":"in Havana"})]; Fake returns `entities=[Cortés(state={"current_state":"in Havana"}), Motecuhzoma(state={"first_mentioned_chapter": 1})]`; assert returned list has length 1 (only Motecuhzoma — new).
       - `test_D_incremental_flags_updated` — prior Cortés state="in Havana"; Fake returns Cortés state="in Veracruz"; assert returned list has length 1 (updated Cortés).
       - `test_E_one_event_emitted` — success path: exactly 1 event with role="entity_extractor", caller_context.chapter_num==1, caller_context.new_cards + caller_context.updated_cards == len(returned). Failure path: exactly 1 error Event with extra.status=="error".
       - `test_F_empty_chapter_raises` — chapter_text="\n\n"; expect EntityExtractorBlocked(reason="empty_chapter") raised WITHOUT calling Fake (assert `fake.call_count == 0`).
       - `test_G_tenacity_exhaustion_3x_fast` — monkeypatch `OpusEntityExtractor._call_opus_inner.retry.wait` to `tenacity.wait_fixed(0)`; Fake raises APIConnectionError every call; expect EntityExtractorBlocked(reason="entity_extraction_failed") raised after 3 attempts; test completes <1s; Event with attempts_made==3.
       - `test_H_prior_cards_injected_into_prompt` — prior_cards=[Cortés(state={"current_state":"in Havana"})]; capture the user prompt Fake received; assert "Cortés" AND "in Havana" substring present (proves prior-summary injection).
  </action>
  <verify>
    <automated>uv run pytest tests/entity_extractor/ -x -q && bash scripts/lint_imports.sh</automated>
  </verify>
  <done>
    - OpusEntityExtractor live, Protocol-conformant.
    - EntityExtractionResponse schema stable and documented.
    - Incremental diff logic verified with 2 targeted tests (C + D).
    - source_chapter_sha stamping enforced (defense-in-depth).
    - 8 tests + 2 schema tests pass.
    - lint + mypy green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpusRetrospectiveWriter (TEST-01 retro) + lint_retrospective() + retry-once-on-lint-fail + tests</name>
  <read_first>
    - src/book_pipeline/interfaces/retrospective_writer.py (Protocol)
    - src/book_pipeline/interfaces/types.py (Retrospective shape: chapter_num, what_worked, what_didnt, pattern, candidate_theses)
    - src/book_pipeline/llm_clients/claude_code.py (messages.create returns CreateResponse.content[0].text — text output path)
    - .planning/phases/04-chapter-assembly-post-commit-dag/04-CONTEXT.md § "RetrospectiveWriter (RETRO-01 via TEST-01, grey-area e)"
  </read_first>
  <behavior>
    - `from book_pipeline.retrospective.opus import OpusRetrospectiveWriter; from book_pipeline.retrospective.lint import lint_retrospective`.
    - `w = OpusRetrospectiveWriter(anthropic_client=..., event_logger=...); isinstance(w, RetrospectiveWriter) is True`.
    - `w.write(chapter_text, chapter_events, prior_retros)` returns a `Retrospective` Pydantic instance populated from Opus's markdown-parsed output.
    - Output markdown structure (written by the caller in Plan 04-04 DAG to `retrospectives/chapter_{NN:02d}.md` — this task only produces the Retrospective model; Plan 04-04 writes it to disk):
      ```markdown
      ---
      chapter_num: <int>
      candidate_theses:
        - id: <string>
          description: <string>
      ---

      # Chapter {NN} Retrospective

      ## What Worked
      <prose>

      ## What Drifted
      <prose>

      ## Emerging Patterns
      <prose>

      ## Open Questions for Next Chapter
      <prose>
      ```
      The `OpusRetrospectiveWriter` returns a `Retrospective(chapter_num=N, what_worked=<str>, what_didnt=<str>, pattern=<str>, candidate_theses=<list>)` where `what_didnt` field carries the "What Drifted" section text, `pattern` carries "Emerging Patterns" text. The "Open Questions" section is captured under `candidate_theses` (each question becomes a `{id: qN, description: <question text>}` dict).
    - `lint_retrospective(retro: Retrospective) -> tuple[bool, list[str]]`:
      - Returns `(True, [])` if retrospective passes lint.
      - Returns `(False, [reasons])` if lint fails.
      - Rule 1: combined text of {what_worked, what_didnt, pattern, candidate_theses descriptions} MUST contain ≥1 match of `re.compile(r"ch\d+_sc\d+")`. Fail reason: `"missing_scene_id_citation"`.
      - Rule 2: combined text MUST contain ≥1 reference to a critic-issue artifact, detected by ANY of: (a) substring match on one of {"historical", "metaphysics", "entity", "arc", "donts"} as a standalone word (bounded via `\b`); OR (b) a chunk_id pattern `chunk_[0-9a-f]+`; OR (c) a quoted string of ≥5 words (using `"[^"]{20,}"` regex) interpreted as evidence quote. Fail reason: `"missing_critic_artifact"`.
    - Retry-on-lint-fail:
      - First invocation: call Opus, parse output, run `lint_retrospective`. If pass → return.
      - If fail: re-invoke Opus ONCE with appended user message `f"The prior draft did not cite specific scene IDs or critic-issue artifacts. Please revise: cite at least one scene_id (format ch<N>_sc<N>) and at least one of: axis name (historical/metaphysics/entity/arc/donts), a chunk_id (format chunk_XXXXXXX), or an evidence quote ≥20 chars."`. Re-run lint.
      - If second lint fail: log warning via `logger.warning("retrospective lint failed twice for chapter %d: %r; committing anyway (ungated)", chapter_num, reasons)`; return the retrospective anyway (commit per CONTEXT.md "ungated: failure → log + skip; next chapter unblocks").
    - One `role="retrospective_writer"` Event per invocation (regardless of retry — ONE event summarizes the call; track `caller_context.lint_retries = 0 | 1` to surface retry pressure). `caller_context = {module, function, chapter_num, events_consumed=len(chapter_events), prior_retros_count, sections_generated=4, lint_pass, lint_retries}`. `extra = {lint_reasons_if_failed: list[str]}`.
    - Tenacity retry on transient errors (same config as EntityExtractor — 3 attempts).
    - Ungated failure: any uncaught exception after 3 tenacity attempts → emit error Event + log warning + RETURN a stub Retrospective with what_worked="(generation failed)" + what_didnt=<exception summary> + pattern="" + candidate_theses=[]. Do NOT raise. CONTEXT.md is explicit: "failure → log + skip; next chapter unblocks".
  </behavior>
  <action>
    1. Create `src/book_pipeline/retrospective/lint.py` (~80 LOC):
       ```python
       """Retrospective output lint rules (TEST-01 success criterion 5)."""
       from __future__ import annotations
       import re
       from book_pipeline.interfaces.types import Retrospective

       _SCENE_ID_RE = re.compile(r"\bch\d+_sc\d+\b")
       _CHUNK_ID_RE = re.compile(r"\bchunk_[0-9a-f]+\b")
       _EVIDENCE_QUOTE_RE = re.compile(r'"[^"]{20,}"')  # quoted ≥20 chars
       _AXIS_WORDS_RE = re.compile(r"\b(historical|metaphysics|entity|arc|donts)\b", re.IGNORECASE)


       def lint_retrospective(retro: Retrospective) -> tuple[bool, list[str]]:
           """Return (pass, reasons). reasons is [] on pass."""
           combined = " ".join([
               retro.what_worked or "",
               retro.what_didnt or "",
               retro.pattern or "",
               " ".join(str(t.get("description", "")) for t in retro.candidate_theses),
           ])
           reasons: list[str] = []
           if not _SCENE_ID_RE.search(combined):
               reasons.append("missing_scene_id_citation")
           has_axis = bool(_AXIS_WORDS_RE.search(combined))
           has_chunk = bool(_CHUNK_ID_RE.search(combined))
           has_quote = bool(_EVIDENCE_QUOTE_RE.search(combined))
           if not (has_axis or has_chunk or has_quote):
               reasons.append("missing_critic_artifact")
           return (not reasons, reasons)


       __all__ = ["lint_retrospective"]
       ```
    2. Create `src/book_pipeline/retrospective/templates/retrospective_system.j2`:
       - "You are the retrospective writer for Our Lady of Champion. Given a committed chapter's markdown, the event log slice for that chapter's drafting cycle, per-scene critic reports, voice-fidelity scores, and per-scene attempt counts, produce a structured retrospective with 4 sections: What Worked / What Drifted / Emerging Patterns / Open Questions for Next Chapter."
       - "You MUST cite at least one specific scene_id (format: `chNN_scNN`) AND at least one of: axis name (historical/metaphysics/entity/arc/donts), a chunk_id (format: `chunk_<hex>`), or an evidence quote of ≥20 characters (in double quotes). Lint will fail otherwise."
       - "Return markdown with YAML frontmatter carrying `chapter_num` + `candidate_theses` (list of {id, description}). Body has 4 H2 sections. Be specific — generic platitudes fail lint."
    3. Create `src/book_pipeline/retrospective/opus.py` (~350 LOC):
       - `OpusRetrospectiveWriter` class:
         - `__init__(self, *, anthropic_client, event_logger, model_id="claude-opus-4-7", max_tokens=6000, temperature=0.4, template_path=DEFAULT_TEMPLATE_PATH)`. Pre-render system prompt; cache `_system_blocks`.
         - `write(self, chapter_text, chapter_events, prior_retros) -> Retrospective`:
           1. Try `_attempt_write(chapter_text, chapter_events, prior_retros, nudge=None)` → Retrospective.
           2. `passed, reasons = lint_retrospective(retro_1)`.
           3. If passed → emit Event with `lint_retries=0, lint_pass=True`; return retro_1.
           4. If not passed → call `_attempt_write(chapter_text, chapter_events, prior_retros, nudge=NUDGE_PROMPT)` → retro_2.
           5. `passed2, reasons2 = lint_retrospective(retro_2)`.
           6. If passed2 → emit Event with `lint_retries=1, lint_pass=True, extra.first_fail_reasons=reasons`; return retro_2.
           7. If not passed2 → `logger.warning(...)`; emit Event with `lint_retries=1, lint_pass=False, extra.lint_reasons_if_failed=reasons2`; return retro_2 anyway (ungated).
         - `_attempt_write(chapter_text, chapter_events, prior_retros, *, nudge) -> Retrospective`:
           1. Build user prompt: `"Chapter text:\n{chapter_text}"` + events-summary block (iterate chapter_events, emit `"[{event.role}] scene={event.caller_context.get('scene_id','?')} latency={event.latency_ms}ms …"`) + prior-retros summary (each `f"Chapter {r.chapter_num}: {r.pattern}"`) + if nudge, append `f"\n\n{nudge}"`.
           2. `_call_opus_inner(messages=...)` — messages.create (free-text markdown), 3 tenacity attempts.
           3. Parse response: extract YAML frontmatter (via split-on `---` heuristic) to get `chapter_num` + `candidate_theses`; extract 4 H2 sections by regex `r"##\s+(What Worked|What Drifted|Emerging Patterns|Open Questions for Next Chapter)\s*\n(.*?)(?=\n##|\Z)"` with `re.DOTALL | re.MULTILINE`.
           4. Build `Retrospective(chapter_num=<parsed>, what_worked=<section>, what_didnt=<drifted section>, pattern=<patterns section>, candidate_theses=[{id: f"q{i+1}", description: <question text>} for i, question in enumerate(split_questions(<open_questions_section>))])`.
           5. On parse failure → raise `RetrospectiveWriterBlocked('parse_failure', chapter_num, raw_output_head=<first 200 chars>)`.
         - Tenacity-exhaustion → `_handle_failure(...)` emits error Event + returns a stub Retrospective `Retrospective(chapter_num, what_worked="(generation failed)", what_didnt=str(exc)[:500], pattern="", candidate_theses=[])` (DOES NOT re-raise — ungated path per CONTEXT.md).
    4. Update `src/book_pipeline/retrospective/__init__.py`: `from book_pipeline.retrospective.opus import OpusRetrospectiveWriter; from book_pipeline.retrospective.lint import lint_retrospective` + `__all__`.
    5. Create `tests/retrospective/test_lint.py` — 4 tests:
       - `test_lint_passes_with_scene_id_and_axis` — Retrospective containing "ch01_sc02 had historical drift" in pattern → passes.
       - `test_lint_fails_missing_scene_id` — Retrospective with axis mention but no chN_scN → fails with reason `missing_scene_id_citation`.
       - `test_lint_fails_missing_critic_artifact` — Retrospective with "ch01_sc01 was OK" but no axis / chunk / quote → fails with `missing_critic_artifact`.
       - `test_lint_passes_with_chunk_id_or_quote` — Retrospective citing "ch01_sc01" + `chunk_abc1234` → passes; separately, "ch01_sc01" + quote of ≥20 chars → passes.
    6. Create `tests/retrospective/test_opus.py` — 6 tests (use `FakeAnthropicClient` returning `CreateResponse(content=[_TextBlock(text=<markdown>)])`):
       - `test_A_protocol_conformance` — `isinstance(w, RetrospectiveWriter) is True`.
       - `test_B_lint_pass_first_try` — Fake returns well-formed markdown citing `ch01_sc01` + axis; writer returns Retrospective, emits 1 Event with `caller_context.lint_retries == 0, lint_pass == True`.
       - `test_C_lint_fail_then_pass_on_retry` — Fake configured to return bad markdown first, good markdown second; writer emits 1 Event with `lint_retries == 1, lint_pass == True, extra.first_fail_reasons == ["missing_critic_artifact"]` (or whatever).
       - `test_D_lint_fail_twice_logs_and_commits` — Fake returns bad markdown both times; writer returns Retrospective anyway; 1 Event with `lint_retries == 1, lint_pass == False, extra.lint_reasons_if_failed != []`; `caplog` captures WARNING "retrospective lint failed twice".
       - `test_E_markdown_parse_shape` — Fake returns a structured markdown document; assert retro.chapter_num == expected, retro.what_worked != "", retro.what_didnt != "", retro.pattern != "", len(retro.candidate_theses) >= 1.
       - `test_F_generation_failure_ungated` — Fake raises APIConnectionError every call (3×); monkeypatch tenacity wait to 0; assert writer RETURNS stub Retrospective (does NOT raise); stub.what_worked == "(generation failed)"; 1 error Event emitted; `caplog` captures a WARNING.
  </action>
  <verify>
    <automated>uv run pytest tests/retrospective/ -x -q && bash scripts/lint_imports.sh</automated>
  </verify>
  <done>
    - OpusRetrospectiveWriter + lint_retrospective live.
    - Retry-once-on-lint-fail pattern exercised by tests C + D.
    - Ungated failure path tested (Test F) — returns stub instead of raising.
    - 6 writer tests + 4 lint tests pass.
    - lint + mypy green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EntityExtractor / RetrospectiveWriter → claude CLI subprocess | Prompts include chapter_text (committed canon). Same shell-injection protection as Plans 03-05 / 04-02 — positional argv, `shell=False`. |
| EntityExtractor → entity-state/*.json writes | Filenames constructed from `chapter_num: int` (int-cast at call boundary). Path traversal blocked. |
| RetrospectiveWriter → retrospectives/*.md writes | Filenames constructed from `chapter_num: int`. Path traversal blocked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | EntityCard source_chapter_sha omitted by LLM | mitigate | Extractor post-processes every card: `card.source_chapter_sha = chapter_sha` override (defense-in-depth; prompt also instructs passthrough). |
| T-04-03-02 | Info disclosure | Chapter markdown leaks into event log | accept | Chapter text is already committed canon (public to pipeline operators); `runs/events.jsonl` is gitignored anyway. Stance identical to Plan 03-05 T-03-05-07. |
| T-04-03-03 | Repudiation | Lint failures silently committed | mitigate | Lint-fail-twice path emits Event with `lint_pass=False, extra.lint_reasons_if_failed=[reasons]` + `logger.warning(...)`. Trail auditable. |
| T-04-03-04 | DoS | Unbounded tenacity on repeated CLI timeout | mitigate | 3 attempts × `wait_exponential(2,2,30)` ≈ 32s ceiling; CLI subprocess timeout 180s each attempt; total worst case ~9min per chapter. Infrequent (1× per chapter commit). |
| T-04-03-05 | EoP | kernel → book_specifics import | mitigate | Plan 04-01 extended `test_kernel_does_not_import_book_specifics` to scan `entity_extractor/` + `retrospective/`. Neither module has book-domain imports. |
| T-04-03-06 | Tampering | Incremental diff silently drops real changes | mitigate | Test C + D prove the diff logic (unchanged filtered out, changed kept). Prior-cards summary in prompt is a subset (compact) — Test H proves the subset reaches Opus. |
| T-04-03-07 | Repudiation | RetrospectiveWriter generation failure unlogged | mitigate | Ungated failure path still emits error Event + WARNING log; stub Retrospective's what_didnt carries the exception summary for Phase 6 digest visibility. |
</threat_model>

<verification>
- `uv run pytest tests/entity_extractor/ tests/retrospective/ -x -q` — all green (~14+ tests).
- `bash scripts/lint_imports.sh` — 2 import-linter contracts + ruff + mypy green on new packages.
- `uv run python -c "from book_pipeline.entity_extractor import OpusEntityExtractor, EntityExtractionResponse; from book_pipeline.retrospective import OpusRetrospectiveWriter, lint_retrospective; from book_pipeline.interfaces.entity_extractor import EntityExtractor; from book_pipeline.interfaces.retrospective_writer import RetrospectiveWriter; print('extractor OK, writer OK')"` works.
- Plans 04-02 (critic) and 04-03 (extractor + retrospective) have DISJOINT files_modified → verified parallel in Wave 2.
</verification>

<success_criteria>
- OpusEntityExtractor satisfies CORPUS-02 (incremental + source_chapter_sha + incremental-diff semantics).
- OpusRetrospectiveWriter satisfies TEST-01 retro side (4-section output + lint rule + retry-once).
- Both components use `build_llm_client(critic_backend_cfg)` via duck-typed `anthropic_client: Any` (matching Plan 03-05/04-02 pattern).
- Lint rule rejects generic boilerplate (success criterion 5 precondition).
- Ungated failure paths documented + tested.
- Plans 04-02 + 04-03 coexist in Wave 2 without file-overlap conflict.
</success_criteria>

<output>
After completion, create `.planning/phases/04-chapter-assembly-post-commit-dag/04-03-SUMMARY.md` summarizing:
- OpusEntityExtractor + schema line counts, test count, tenacity=3 attempts rationale vs critic's 5.
- OpusRetrospectiveWriter line count, lint rule detection patterns, retry-once-then-commit behavior.
- Any deviation from CONTEXT.md (e.g., field name choices).
- Commit SHA(s).
</output>
