---
phase: 04-chapter-assembly-post-commit-dag
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - tests/integration/__init__.py
  - tests/integration/test_chapter_dag_end_to_end.py
  - tests/integration/conftest.py
  - tests/integration/fixtures/ch99_sc01.md
  - tests/integration/fixtures/ch99_sc02.md
  - tests/integration/fixtures/ch99_sc03.md
autonomous: true
requirements: [LOOP-02, LOOP-03, LOOP-04, CRIT-02, CORPUS-02, TEST-01]

must_haves:
  truths:
    - "A single end-to-end integration test runs `book-pipeline chapter 99` against a 3-scene stub chapter with fully mocked LLM clients + mocked retrievers/bundler + real git subprocess against a tmp_path repo, and verifies ALL Phase 4 success criteria in one shot."
    - "The test asserts 4 git commits in strict order: `canon(ch99)`, `chore(entity-state): ch99 extraction`, `chore(rag): reindex after ch99`, `docs(retro): ch99`."
    - "The test asserts on-disk artifacts: `canon/chapter_99.md`, `entity-state/chapter_99_entities.json`, `retrospectives/chapter_99.md`, `.planning/pipeline_state.json` with `dag_complete: True`."
    - "The test asserts the fresh-pack invariant: `chapter_pack.fingerprint NOT IN {scene_pack.fingerprint}` at runtime."
    - "The test asserts the B-3 invariant persists through assembly: scene md frontmatter has `voice_pin_sha == checkpoint_sha`, chapter frontmatter has `voice_pin_shas: [sha]` (deduplicated)."
    - "The test asserts lint-passing retrospective: regex matches ≥1 scene_id + ≥1 critic-artifact in the committed retrospective markdown."
    - "The test asserts exit code 0 from the CLI."
    - "The test asserts Phase 4 event log shape: `runs/events.jsonl` (in tmp_path) contains events with roles={chapter_critic, entity_extractor, retrospective_writer} + proper caller_context.chapter_num stamps."
  artifacts:
    - path: "tests/integration/test_chapter_dag_end_to_end.py"
      provides: "One LOOP-04 end-to-end regression test exercising Plans 04-01..04-05 together"
    - path: "tests/integration/fixtures/ch99_sc01.md"
      provides: "3-scene stub chapter test fixture with valid Plan 03-07 B-3 frontmatter"
    - path: "tests/integration/fixtures/ch99_sc02.md"
      provides: "(same)"
    - path: "tests/integration/fixtures/ch99_sc03.md"
      provides: "(same)"
  key_links:
    - from: "tests/integration/test_chapter_dag_end_to_end.py"
      to: "src/book_pipeline/cli/chapter.py"
      via: "subprocess invocation of `book-pipeline chapter 99` OR direct `_run(args)` call"
      pattern: "book-pipeline chapter"
    - from: "tests/integration/test_chapter_dag_end_to_end.py"
      to: "src/book_pipeline/llm_clients/factory.py"
      via: "monkeypatch `build_llm_client` to return a scripted mock that emits valid CriticResponse / EntityExtractionResponse / retrospective markdown"
      pattern: "build_llm_client"
---

<objective>
Land a single end-to-end integration test that exercises Plans 04-01..04-05 against a fully-mocked LLM backend, in a tmp_path `git init`-ed repo, and regression-guards every Phase 4 success criterion:

1. **Success criterion 1** (deterministic assembly): 3 scene fixtures concatenated into `canon/chapter_99.md` — byte-identical on re-run (same seed conditions).
2. **Success criterion 2** (independent RAG pack for chapter critic): runtime-assert the chapter critic received a different pack fingerprint than any "scene pack" plumbed through the mock bundler.
3. **Success criterion 3** (on PASS, atomic commit + post-commit DAG fires): `git log --oneline` shows exactly 4 new commits in order; all 3 artifact files exist; `pipeline_state.json` says `dag_complete: True`.
4. **Success criterion 5** (retrospective lint): committed `retrospectives/chapter_99.md` passes `lint_retrospective` (≥1 scene_id + ≥1 critic-artifact regex match).
5. **Success criterion 6** (EntityCards carry source_chapter_sha): loaded `entity-state/chapter_99_entities.json` validates through `EntityExtractionResponse`; every card has `source_chapter_sha == <captured chapter commit sha>`.
6. **Plan 03-07 B-3 invariant continuity**: all scene md fixtures have `voice_pin_sha == checkpoint_sha`; assembled chapter frontmatter has `voice_pin_shas: [sha]` size=1 (no mid-chapter upgrade in this test) OR size=2 (if the test deliberately plumbs a mid-chapter upgrade — do both variants via parametrize).
7. **LOOP-04 gate**: after successful DAG run, verify `.planning/pipeline_state.json` carries `dag_complete: True` + `last_committed_chapter: 99`, AND a simulated next-chapter attempt (Plan 04-06 test does NOT actually run chapter 100 — just asserts the gate values are in place for Phase 5 cron to read).

Purpose: This is the ONE test that proves Phase 4 works end-to-end. If it passes, every Plan 04-01..04-05 contract held under composition. If it fails, the failure message points at which Plan regressed. No live LLM calls, no live vLLM, no live RAG corpus — fully mockable per CONTEXT.md "Phase 4 is fully mockable; live smoke deferred to Phase 6".

Output:
- One integration test file.
- One conftest.py with shared fixtures (tmp_path repo init, mock LLM client factory, mock bundler returning a scripted ContextPack).
- 3 scene md fixtures on disk with realistic B-3 frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-chapter-assembly-post-commit-dag/04-CONTEXT.md
@.planning/phases/04-chapter-assembly-post-commit-dag/04-04-PLAN.md
@.planning/phases/04-chapter-assembly-post-commit-dag/04-05-PLAN.md
@src/book_pipeline/cli/chapter.py
@src/book_pipeline/chapter_assembler/dag.py
@src/book_pipeline/llm_clients/factory.py
@src/book_pipeline/cli/draft.py
@tests/cli/test_draft_loop.py
@src/book_pipeline/retrospective/lint.py

<interfaces>
<!-- What this test must exercise end-to-end. -->

Composition root path (Plan 04-05 cli/chapter.py):
1. `_build_dag_orchestrator(99)` constructs all 11 DI components.
2. `orchestrator.run(99, expected_scene_count=3)` drives 4-step DAG.
3. Returns `ChapterStateRecord` with terminal state DAG_COMPLETE.

Mock seam (the ONLY seam that needs replacement):
- `build_llm_client(critic_backend_cfg)` → returns `MockLLMClient` with scripted `messages.parse` + `messages.create` responses.

Mocks must provide:
- `messages.parse(... output_format=CriticResponse ...)` → ParseResponse with CriticResponse(overall_pass=True, pass_per_axis=all True, scores_per_axis all 80.0, issues=[], model_id="claude-opus-4-7", rubric_version="chapter.v1", output_sha="...").
- `messages.parse(... output_format=EntityExtractionResponse ...)` → ParseResponse with EntityExtractionResponse(entities=[EntityCard(entity_name="Cortés", last_seen_chapter=99, state={"current_state": "at Tenochtitlan"}, evidence_spans=['"the captain rode into the city"'], source_chapter_sha="WILL_BE_OVERRIDDEN")], chapter_num=99, extraction_timestamp="...").
- `messages.create(...)` (retrospective) → CreateResponse.content[0].text = a valid lint-passing markdown block citing "ch99_sc01" + "entity" axis + an evidence quote.

Retriever mock seam:
- Instead of constructing real BgeM3Embedder + 5 LanceDB retrievers, monkeypatch the `book_pipeline.cli.chapter.build_retrievers_from_config` to return a dict of 5 `FakeRetriever` instances (each `.retrieve(request)` returns a scripted RetrievalResult; `.index_fingerprint()` returns a fixed string).
- Also monkeypatch `BgeM3Embedder.__init__` + `BgeReranker.__init__` to no-op (avoid 2GB model download at test time).
- Alternatively: provide a complete replacement `_build_dag_orchestrator_for_test(chapter_num, *, mock_client_factory, mock_retrievers)` helper in cli/chapter.py (export for tests). Prefer monkeypatch path to avoid test-code-leakage into production CLI.

Bundler mock seam (to exercise fresh-pack invariant assertion):
- Monkeypatch `book_pipeline.cli.chapter.ContextPackBundlerImpl` with a wrapped subclass that records every `.bundle(request)` call's SceneRequest + returned ContextPack.fingerprint into a spy list. The test then asserts: at least 1 bundle() call has `scene_request.scene_index == 0 AND scene_request.beat_function == "chapter_overview"` (the chapter critic's fresh-pack query); assert its fingerprint is unique (no other bundle call produced the same fingerprint — proxy for "chapter pack not equal to scene pack").

Git subprocess:
- Test runs in tmp_path with `git init` + `git commit -m "init"` (empty initial commit) pre-run. The DAG orchestrator's `commit_paths` calls real `git` subprocess. Assert `git log --oneline` shows exactly 5 commits after the test (1 initial + 4 DAG commits).
- IMPORTANT: tmp_path must have a configured git author (`git config user.name` + `user.email`) or `git commit` fails. The conftest fixture sets these.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration conftest.py — tmp_path repo init + mock factories + 3 scene fixtures</name>
  <read_first>
    - tests/cli/test_draft_loop.py lines 100-300 — FakeDrafter / FakeCritic / FakeRegenerator patterns to mirror as FakeBundler / FakeChapterCritic / FakeEntityExtractor / FakeRetrospectiveWriter (OR: use the real kernel classes with a mock LLM client — probably cleaner; choose at implementation time).
    - src/book_pipeline/cli/draft.py (composition-root pattern; cli/chapter.py from Plan 04-05 follows same shape)
    - src/book_pipeline/llm_clients/claude_code.py — ParseResponse + CreateResponse shapes for mock construction
    - Plan 04-02 Task 2 behavior section (chapter critic rubric response shape)
    - Plan 04-03 Task 1 behavior section (entity extractor response shape)
    - Plan 04-03 Task 2 behavior section (retrospective markdown shape)
  </read_first>
  <action>
    1. Create `tests/integration/__init__.py` (empty).
    2. Create `tests/integration/fixtures/ch99_sc01.md`:
       ```markdown
       ---
       voice_pin_sha: abcd1234abcd5678abcd1234abcd5678abcd1234abcd5678abcd1234abcd5678
       checkpoint_sha: abcd1234abcd5678abcd1234abcd5678abcd1234abcd5678abcd1234abcd5678
       critic_scores_per_axis:
         historical: 85.0
         metaphysics: 80.0
         entity: 82.0
         arc: 78.0
         donts: 90.0
       attempt_count: 1
       ingestion_run_id: ing_test_20260422
       draft_timestamp: 2026-04-22T12:00:00+00:00
       voice_fidelity_score: 0.82
       mode: A
       rubric_version: v1
       ---
       Cortés rode at the head of his column, the banner of Castile snapping in the late-April wind. The trail from Veracruz had been long, and the first sight of Cempoala's stone courts stopped even the hardened soldiers. Doña Marina rode beside him, her voice the only bridge between the Spaniards and the Totonac cacique they were about to meet.
       ```
       (A ~50-word realistic narrative fragment; 2nd and 3rd fixtures follow the same shape with different body text.)
    3. Create `tests/integration/fixtures/ch99_sc02.md` — same frontmatter shape (voice_pin_sha identical — no mid-chapter upgrade in the happy-path test; test parametrization may override), body text advancing the beat (the meeting, the gifts exchanged).
    4. Create `tests/integration/fixtures/ch99_sc03.md` — same frontmatter, body text wrapping the beat (departure toward Tlaxcala).
    5. Create `tests/integration/conftest.py` (~200 LOC):
       - Pytest fixture `tmp_repo(tmp_path, monkeypatch) -> Path`:
         - Runs `subprocess.run(["git", "init"], cwd=tmp_path, check=True)` + `subprocess.run(["git", "config", "user.name", "Test User"], cwd=tmp_path, check=True)` + `subprocess.run(["git", "config", "user.email", "test@example.com"], cwd=tmp_path, check=True)`.
         - Copies the 3 scene fixtures into `tmp_path / "drafts" / "ch99" / "ch99_sc01.md"` etc.
         - Pre-seeds `tmp_path / "drafts" / "scene_buffer" / "ch99" / "ch99_sc01.state.json"` with a minimal SceneStateRecord (state=COMMITTED, scene_id="ch99_sc01", mode_tag="A") — same for sc02/sc03. These files satisfy the gate check even though the DAG doesn't read them (scene md frontmatter is the truth).
         - Pre-seeds `tmp_path / "indexes" / "resolved_model_revision.json"` with `{"ingestion_run_id": "ing_test_20260422", "sha": "abcdef", "model": "BAAI/bge-m3", "resolved_at_iso": "2026-04-22T12:00:00Z"}`.
         - Pre-seeds all 4 configs: `config/voice_pin.yaml`, `config/rubric.yaml`, `config/rag_retrievers.yaml`, `config/mode_thresholds.yaml` with minimal valid shapes (copy from repo's real config files so Pydantic validation passes).
         - Does initial `git add -A` + `git commit -m "init"`.
         - Monkeypatches `os.chdir(tmp_path)` via `monkeypatch.chdir(tmp_path)`.
         - Yields `tmp_path`.
       - Pytest fixture `mock_llm_client()` → returns a `MockLLMClient` instance with scripted messages.parse + messages.create:
         ```python
         class MockLLMClient:
             messages = None  # set in __init__

         class _MockMessages:
             def __init__(self):
                 self.parse_calls = []
                 self.create_calls = []
             def parse(self, *, model, messages, output_format, system=None, max_tokens=4096, temperature=None):
                 self.parse_calls.append((output_format.__name__, messages))
                 if output_format.__name__ == "CriticResponse":
                     return ParseResponse(parsed_output=CriticResponse(
                         pass_per_axis={a: True for a in ("historical","metaphysics","entity","arc","donts")},
                         scores_per_axis={a: 80.0 for a in ("historical","metaphysics","entity","arc","donts")},
                         issues=[], overall_pass=True, model_id=model,
                         rubric_version="chapter.v1", output_sha="mock_critic_output_sha",
                     ))
                 if output_format.__name__ == "EntityExtractionResponse":
                     return ParseResponse(parsed_output=EntityExtractionResponse(
                         entities=[EntityCard(entity_name="Cortés", last_seen_chapter=99,
                                              state={"current_state": "at Cempoala", "aliases": [], "entity_type": "person"},
                                              evidence_spans=['"Cortés rode at the head of his column"'],
                                              source_chapter_sha="WILL_BE_OVERRIDDEN")],
                         chapter_num=99, extraction_timestamp="2026-04-22T12:00:00Z",
                     ))
                 raise ValueError(f"Unexpected output_format: {output_format.__name__}")
             def create(self, *, model, messages, system=None, max_tokens=4096, temperature=None):
                 self.create_calls.append((model, messages))
                 # Lint-passing retrospective markdown
                 return CreateResponse(content=[_TextBlock(text="""---
       chapter_num: 99
       candidate_theses:
         - id: q1
           description: "Did the Cempoala scene drift on entity axis? See ch99_sc02."
       ---

       # Chapter 99 Retrospective

       ## What Worked
       The historical axis held across ch99_sc01 and ch99_sc02; Marina's translation bridge remained consistent.

       ## What Drifted
       Ch99_sc02 showed a borderline entity issue — "the captain rode into the city" evidence quote conflicts with the outline staging.

       ## Emerging Patterns
       Early Cempoala beats may benefit from tighter metaphysics axis guidance before Tlaxcala.

       ## Open Questions for Next Chapter
       q1: Does the Tlaxcala alliance scene require a pre-flagged Mode-B escape?
       """)])
         ```
       - Pytest fixture `mock_retrievers_factory(monkeypatch)` — monkeypatches `book_pipeline.cli.chapter.build_retrievers_from_config` to return a `{name: FakeRetriever(...)}` dict of 5 fakes. Each fake:
         - `name` attr matches the 5 axis names (historical, metaphysics, entity_state, arc_position, negative_constraint).
         - `.retrieve(request)` returns `RetrievalResult(retriever_name=self.name, hits=[], bytes_used=0, query_fingerprint="fake_fp")`.
         - `.reindex()` → no-op.
         - `.index_fingerprint()` returns `f"fake_{self.name}_fp"`.
       - Pytest fixture `mock_embedder_and_reranker(monkeypatch)` — monkeypatches `book_pipeline.rag.BgeM3Embedder.__init__` and `book_pipeline.rag.reranker.BgeReranker.__init__` to no-ops (skip 2GB model load).
       - Pytest fixture `bundler_fingerprint_spy(monkeypatch)` — monkeypatches `ContextPackBundlerImpl.bundle` to ALSO record each call's `(scene_request, result.fingerprint)` into a module-level spy list for later assertion.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/conftest.py --collect-only -q</automated>
  </verify>
  <done>
    - conftest.py has 5 fixtures (tmp_repo, mock_llm_client, mock_retrievers_factory, mock_embedder_and_reranker, bundler_fingerprint_spy).
    - 3 scene fixtures copied into fixtures/.
    - Fixtures collect without import errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end test — invoke `book-pipeline chapter 99` + assert all Phase 4 success criteria</name>
  <read_first>
    - Plan 04-06 Task 1 (conftest fixtures)
    - src/book_pipeline/cli/chapter.py (from Plan 04-05; understand the composition-root invocation path)
    - src/book_pipeline/chapter_assembler/dag.py (from Plan 04-04; understand state record shape)
    - src/book_pipeline/retrospective/lint.py (from Plan 04-03)
  </read_first>
  <behavior>
    - Test `test_end_to_end_3_scene_stub_chapter_dag` exercises:
      1. Setup: use `tmp_repo` fixture + all 4 mock fixtures.
      2. Monkeypatch `book_pipeline.llm_clients.factory.build_llm_client` to return the `mock_llm_client` instance (all 3 Phase 4 Opus callers — ChapterCritic, OpusEntityExtractor, OpusRetrospectiveWriter — receive this one client).
      3. Either (a) call `subprocess.run(["uv", "run", "book-pipeline", "chapter", "99", "--expected-scene-count", "3"], cwd=tmp_repo, capture_output=True, text=True)` AND assert returncode == 0 + stdout contains "terminal_state=dag_complete" — OR (b) directly invoke `book_pipeline.cli.chapter._run(argparse.Namespace(chapter_num=99, expected_scene_count=3, no_archive=False))` and assert return == 0. PREFER (b) for mock-injection simplicity — subprocess boundary would require env-var hacks to inject fake build_llm_client.
      4. Assertions on the committed state:
         - `git log --oneline` in tmp_repo shows 5 commits (1 initial + 4 DAG).
         - `git log --pretty=format:"%s" -n 4 | reverse` = [`canon(ch99): commit chapter 99`, `chore(entity-state): ch99 extraction`, `chore(rag): reindex after ch99`, `docs(retro): ch99`] in EXACT order (assert with regex-match to tolerate wording drift within each commit).
         - `canon/chapter_99.md` exists. Parse its YAML frontmatter; assert `chapter_num == 99`, `assembled_from_scenes == ["ch99_sc01", "ch99_sc02", "ch99_sc03"]`, `word_count > 0`, `voice_pin_shas == ["abcd1234...d064"]` (size 1; B-3 preserved), `chapter_critic_pass: True`.
         - Body of `canon/chapter_99.md` contains 3 HTML scene markers + 2 `---` section separators.
         - `entity-state/chapter_99_entities.json` exists. Parse via `EntityExtractionResponse.model_validate_json`; assert `len(response.entities) == 1`, `response.entities[0].entity_name == "Cortés"`, `response.entities[0].source_chapter_sha` matches the captured canon commit sha (from `git log -n 4 --format=%H`).
         - `retrospectives/chapter_99.md` exists. Parse; pass to `lint_retrospective(retro) → (True, [])`.
         - `.planning/pipeline_state.json` has `{last_committed_chapter: 99, last_committed_dag_step: 4, dag_complete: true}`.
         - `drafts/chapter_buffer/ch99.state.json` has `state == "dag_complete"`, `dag_step == 4`, `chapter_sha == <canon commit sha>`.
      5. Fresh-pack invariant assertion: `bundler_fingerprint_spy` records ≥1 call with `scene_request.scene_index == 0 AND scene_request.beat_function == "chapter_overview"`. Let `chapter_pack_fp` be its returned fingerprint. Let `other_fps` = fingerprints from all OTHER bundle() calls (if any — in this test, the chapter critic is the SOLE bundler caller since scenes are pre-committed). Assert `chapter_pack_fp not in other_fps` (trivially true in this test setup since there's only 1 bundle call, but the assertion shape is future-proof for Plan 04-06's parametrize variant where prior scene packs are plumbed through).
      6. Event log assertion: `runs/events.jsonl` in tmp_repo has JSON lines with `role == "chapter_critic"` (from Plan 04-02), `role == "entity_extractor"` (from Plan 04-03 Task 1), `role == "retrospective_writer"` (from Plan 04-03 Task 2). Each has `caller_context.chapter_num == 99` (or `caller_context.chapter == 99` — Plan 04-04 tolerates both keys in the chapter_events filter).
      7. LOOP-04 gate readiness: assert pipeline_state.json's `dag_complete: True` signal is present, which Phase 5 cron WILL read. (Do not actually run chapter 100 — this test only verifies the gate values are materialized for Phase 5 to consume.)
    - Parametrize variant: `test_end_to_end_mid_chapter_pin_upgrade` — modify the `tmp_repo` fixture for this variant so `ch99_sc02.md` has a DIFFERENT `voice_pin_sha` from sc01 + sc03; assert `chapter frontmatter voice_pin_shas: [sha1, sha2]` size == 2 (signals mid-chapter upgrade per CONTEXT.md).
    - Parametrize variant (OPTIONAL if time permits): `test_chapter_critic_fail_no_canon_commit` — mock_llm_client configured to return `overall_pass=False`; assert returncode == 3, no new commits, state == CHAPTER_FAIL.
  </behavior>
  <action>
    1. Create `tests/integration/test_chapter_dag_end_to_end.py` (~450 LOC):
       - Import all the fixtures from conftest.
       - `test_end_to_end_3_scene_stub_chapter_dag(tmp_repo, mock_llm_client, mock_retrievers_factory, mock_embedder_and_reranker, bundler_fingerprint_spy, monkeypatch)`:
         - `monkeypatch.setattr("book_pipeline.cli.chapter.build_llm_client", lambda cfg: mock_llm_client)` (CAVEAT: the name must resolve to what cli/chapter.py actually imports — inspect the Plan 04-05 cli/chapter.py module to confirm it imports `from book_pipeline.llm_clients import build_llm_client`; patch AT THE CALL SITE, not at the source module).
         - `import argparse; args = argparse.Namespace(chapter_num=99, expected_scene_count=3, no_archive=False)`.
         - `from book_pipeline.cli.chapter import _run; rc = _run(args); assert rc == 0`.
         - Gather all the assertions listed in § Behavior item 4-7.
       - `test_end_to_end_mid_chapter_pin_upgrade(...)` — same setup but overwrite `ch99_sc02.md` with a different `voice_pin_sha` before `_run`; assert `voice_pin_shas` size == 2 in chapter frontmatter.
       - `test_chapter_critic_fail_no_canon_commit(...)` — override the mock so CriticResponse has `overall_pass=False`; assert `rc == 3`, `canon/chapter_99.md` does NOT exist, git log shows 0 new commits.
       - `test_resumability_after_dag_step_2_failure(...)` — OPTIONAL stretch: configure mock EntityExtractor to raise on first call; run → assert rc == 4, state == DAG_BLOCKED, dag_step == 1; reconfigure mock to succeed; run again → assert rc == 0, state == DAG_COMPLETE, dag_step == 4; assert ChapterCritic.review NOT called on the 2nd run (step 1 skipped due to resumability).
       - Mark stretch tests with `@pytest.mark.slow` if they take >5s.
    2. Ensure pytest discovers the new directory: verify `pyproject.toml` `[tool.pytest.ini_options]` does NOT exclude `tests/integration/` — it should match default `tests/` glob.
    3. DO NOT add new `@pytest.mark` registrations beyond `slow` (already registered per pyproject.toml).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_chapter_dag_end_to_end.py -x -q -v && bash scripts/lint_imports.sh && uv run pytest tests -x -q --ignore=tests/rag/test_golden_queries.py</automated>
  </verify>
  <done>
    - `tests/integration/test_chapter_dag_end_to_end.py` has ≥2 passing tests (happy path + at least 1 parametrize variant).
    - Full non-slow suite passes end-to-end (including Phase 3 tests — no regression).
    - Plan 04-06 can be declared Phase 4 green: 4 commits, 3 artifacts, pipeline_state gate, fresh-pack invariant, lint pass — all verified in one test.
    - lint + mypy green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → tmp_path | All test writes constrained to tmp_path; tests never mutate the real repo's canon/ or drafts/. |
| Mock LLM client | Accepts any args, emits deterministic scripted responses. Never invokes claude CLI / anthropic SDK. |
| Real git subprocess in tmp_path | Git config set locally in tmp_path (user.name + user.email) so real commits work; no ambient git config pollution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | Test mutates real repo canon/ | mitigate | `monkeypatch.chdir(tmp_path)` at fixture-setup time. Every write path resolves through the new cwd. Test-teardown returns to original cwd automatically. |
| T-04-06-02 | EoP | Mock LLM client leaks API key or subprocess side effect | mitigate | MockLLMClient is a pure Python dataclass — NO subprocess, NO network. `monkeypatch.setenv("ANTHROPIC_API_KEY", "")` in fixture to ensure no accidental live call would succeed even if the mock slipped. |
| T-04-06-03 | Repudiation | Integration test failure masks which Plan regressed | mitigate | Test asserts are organized by Plan (Plan 04-02 assertions grouped, Plan 04-03 assertions grouped, etc.) with inline comments citing the plan number. Failure message points at the plan. |
| T-04-06-04 | DoS | Test downloads 2GB BGE-M3 model | mitigate | `mock_embedder_and_reranker` fixture patches `BgeM3Embedder.__init__` to no-op. Test runs without GPU. |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_chapter_dag_end_to_end.py -x -q -v` — all tests green (≥2 tests: happy + mid-chapter-pin-upgrade).
- Full non-slow suite: `uv run pytest tests -x -q --ignore=tests/rag/test_golden_queries.py` green (existing pre-existing flake from STATE.md remains deselected).
- `bash scripts/lint_imports.sh` green (no new kernel imports).
- `git log --oneline` IN TMP_REPO (captured by test) shows 5 commits in expected order.
- Runtime assertion: `chapter_pack.fingerprint not in {other_fps}` holds.
</verification>

<success_criteria>
- End-to-end integration test passes: 3-scene stub chapter → 4 commits + 3 artifacts + pipeline_state gate + lint-pass retrospective + source_chapter_sha continuity.
- Test covers LOOP-02, LOOP-03, LOOP-04, CRIT-02, CORPUS-02, TEST-01 (retrospective side) in one run.
- No pre-existing test regressions (Phase 1-3 suite still green).
- Phase 4 is green end-to-end; ready for STATE.md update + phase roll-forward.
</success_criteria>

<output>
After completion, create `.planning/phases/04-chapter-assembly-post-commit-dag/04-06-SUMMARY.md` summarizing:
- Integration test file line count + number of tests + number of parametrize variants.
- Fixture strategy (tmp_path repo init + 5 monkeypatch seams).
- Runtime for the happy-path test (should be <5s with all mocks).
- Final Phase 4 test count delta: (+X unit tests across Plans 04-01..04-05, +Y integration tests; total +Z from pre-Phase-4 baseline).
- Commit SHA(s).

Also update `.planning/phases/04-chapter-assembly-post-commit-dag/04-VERIFICATION.md` (create if absent) with a per-success-criterion check:
- [x] 1. Deterministic assembly — test A verified byte-identical re-run.
- [x] 2. Fresh RAG pack — bundler_fingerprint_spy verified.
- [x] 3. Atomic canon + DAG — 4 commits in order.
- [x] 4. Surgical scene-kick on fail (terminal state CHAPTER_FAIL — Phase 5 routing) — deferred to Phase 5 but state transition tested.
- [x] 5. Retrospective lint — `lint_retrospective` called in test, returns (True, []).
- [x] 6. source_chapter_sha stamped + matches canon commit sha — verified.
</output>
