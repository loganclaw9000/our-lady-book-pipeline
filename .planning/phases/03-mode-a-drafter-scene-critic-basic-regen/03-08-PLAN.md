---
phase: 03-mode-a-drafter-scene-critic-basic-regen
plan: 08
type: execute
wave: 7
depends_on: ["07"]
files_modified: []
autonomous: false
requirements: [REGEN-01]
tags: [human-verify, end-to-end-smoke, ch01-sc01, gpu-real-vllm, real-anthropic, real-corpus, phase-3-closure]

must_haves:
  truths:
    - "End-to-end smoke on REAL infrastructure validates the Phase 3 scene loop: `book-pipeline draft ch01_sc01` against live vLLM (paul-v6-qwen3-32b-lora on :8002) + live Anthropic Opus 4.7 + live RAG indexes (Phase 2 baseline ingestion_run_id=ing_20260422T082448725590Z_2264c687). Outcome: EITHER COMMITTED (drafts/ch01/ch01_sc01.md exists with all 9 frontmatter keys + readable prose) OR HARD_BLOCKED with a documented blocker (failed_critic_after_R_attempts | training_bleed — both acceptable Phase 3 endings; they prove the loop exercises the full state machine)."
    - "Task is gated behind a pre-flight checklist operator executes before Claude spends the ~$0.6 Anthropic budget: vLLM health + SHA handshake, ANTHROPIC_API_KEY present, GPU VRAM sufficient, RAG indexes populated, voice_pin.yaml has real V6 SHA, anchor_set_v1.yaml + embeddings.parquet committed."
    - "Acceptable Phase 3 PASS outcomes: (a) PASS-COMMIT: drafts/ch01/ch01_sc01.md exists with all 9 frontmatter keys + scene_text matches voice, (b) PASS-HARDBLOCK-FAIL-CRITIC: state.json HARD_BLOCKED + 'failed_critic_after_R_attempts' — expected for a V6 model on a complex beat, (c) PASS-HARDBLOCK-TRAINING-BLEED: state.json HARD_BLOCKED + 'training_bleed' — V-2 live, catch a real bleed. Documented 12-gram gets recorded in 03-08-SUMMARY."
    - "FAIL outcomes require operator re-run after infra fix: HARD_BLOCKED('mode_a_unavailable') (vLLM issue), HARD_BLOCKED('critic_blocked:anthropic_unavailable') (Anthropic auth/rate), HARD_BLOCKED('anchor_set_drift') (Plan 03-02 state out of sync), unhandled Python traceback outside the HARD_BLOCKED taxonomy (integration gap — fix before Phase 4)."
    - "Task 3 does NOT ship new code files — it is a human-verify checkpoint that EXERCISES the code shipped by Plans 03-01 through 03-07. Claude writes 03-08-SUMMARY.md after the operator signals per <resume-signal>."
    - "Budget sanity: ch01_sc01 draft ~8-12s at ~1500 Qwen3-32B tokens; critic call ~30-45s at ~5000 input + ~1500 output tokens; regen ~30-60s if triggered. Total wall-time 3-10 minutes for R=3 (1 draft + up to 4 critic + up to 3 regen = 8 LLM calls max). Total Anthropic spend ~$0.6 (4 critics × ~$0.12 + 3 regens × ~$0.05)."
  artifacts: []
  key_links:
    - from: "Plan 03-08 human-verify outcome"
      to: ".planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-08-SUMMARY.md"
      via: "Operator signals via <resume-signal>; Claude documents outcome + token counts + wall time + $ spend + any deferred issues"
      pattern: "03-08-SUMMARY.md"
    - from: "drafts/ch01/ch01_sc01.md OR drafts/scene_buffer/ch01/ch01_sc01.state.json"
      to: "Phase 4 ChapterAssembler input"
      via: "COMMITTED scenes feed Phase 4 via drafts/ch{NN}/; HARD_BLOCKED state is Phase 5 REGEN-03 input"
      pattern: "drafts/ch01/ch01_sc01.md"
---

<objective>
Execute the Phase 3 end-to-end smoke as a human-verified forcing function: `book-pipeline draft ch01_sc01` against REAL vLLM + REAL Anthropic + REAL RAG indexes + REAL voice anchors. Validates the whole Phase 3 architecture on a live scene BEFORE Phase 4 chapter-assembly work begins.

Purpose:
- Phase 3 integration forcing function: all kernel tests green from Plans 03-01..03-07 is necessary but not sufficient. Real infra reveals wiring errors mocked tests can't catch (e.g., Anthropic auth config, vLLM LoRA adapter SHA mismatch, BGE-M3 revision drift, anchor_set_sha mismatch).
- OBS-03 validation: voice_fidelity_score attached to a real Mode-A Event against real anchor centroid.
- CRIT-04 validation: audit record lands with real Anthropic Opus payload + cache_control hit (attempt 2+ shows cached_input_tokens > 0).
- V-2 validation: memorization gate runs against the real 10,751-row training corpus on real V6 output.
- V-3 validation: checkpoint_sha on every drafter Event matches voice_pin.yaml pinned SHA.

Why split from 03-07 (B-1 closure): the original 03-06 bundled regenerator impl + CLI composition + real-smoke human-verify into one plan. The real smoke has operator cost (~$0.6 Anthropic) + requires operator pre-flight. Separating 03-08 into its own plan gives the smoke a clean boundary — operator can re-run 03-08 independently if infra drifts between the initial smoke and later work.

Out of scope:
- Any code changes. Plan 03-08 is pure execution + documentation. If the smoke reveals a code bug, operator creates a gap-closure plan in a revision pass (not inline here).
- Phase 4 ChapterAssembler.
- Phase 5 Mode-B escalation on HARD_BLOCKED('failed_critic_after_R_attempts') — this plan documents the terminal state; Phase 5 wires the re-route edge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-CONTEXT.md
@.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-01-PLAN.md
@.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-02-PLAN.md
@.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-03-PLAN.md
@.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-04-PLAN.md
@.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-05-PLAN.md
@.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-06-PLAN.md
@.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-07-PLAN.md

<smoke_execution_plan>
Task 1 executes the end-to-end smoke as a human-verify checkpoint. Pre-conditions operator verifies BEFORE authorizing resume:

1. vLLM serving paul-v6-qwen3-32b-lora on :8002:
   - `book-pipeline vllm-bootstrap --enable --start` (from Plan 03-03) OR operator-managed systemd.
   - `curl -s http://127.0.0.1:8002/v1/models | jq '.data[].id'` shows `paul-voice`.
   - boot_handshake passes (SHA match).
2. ANTHROPIC_API_KEY set in env (operator-managed).
3. GPU state check: `nvidia-smi` shows enough VRAM for Qwen3-32B bf16 (~65GB) + BGE-M3 fp16 (~2GB) coexistence on 96GB GB10. If BGE-M3 doesn't fit alongside, operator may need to pause vllm-qwen122 (MEMORY.md rule: `systemctl --user stop vllm-qwen122.service` — NOT docker kill).
4. RAG indexes populated (Phase 2 Plan 02-06 baseline ingestion_run_id=ing_20260422T082448725590Z_2264c687). If indexes/ empty, operator runs `book-pipeline ingest` first.
5. voice_pin.yaml has real V6 SHA (Plan 03-01 Task 3 executed).
6. anchor_set_v1.yaml + indexes/voice_anchors/embeddings.parquet committed (Plan 03-02 Task 2 executed).

Smoke expectations (budget sanity from CONTEXT):
- ch01_sc01 draft: ~1000 words target → ~1500 Qwen3-32B tokens output at ~40-60 tok/s → ~8-12s per draft.
- Critic call: ~5000 input tokens (rubric + few-shot + scene + retrievals) + ~1500 output (structured JSON) → ~30-45s per critic call at Opus 4.7 latency.
- Regen (if triggered): similar to draft but against Opus → ~30-60s per regen call.
- Total wall-time: 1 draft + 1 critic + up to 3 regens + 3 more critics = up to 8 LLM calls ≈ 3-10 minutes.
- Total $ spend: ~$0.08-0.12/critic × 4 (1 initial + 3 regen) = ~$0.4-0.5 + regen calls ($0.05/regen × 3) = ~$0.6 per smoke run.

Outcomes acceptable for smoke PASS:
- PASS-COMMIT: drafts/ch01/ch01_sc01.md exists with YAML frontmatter; 5-axis critic scores >=70 each; voice_fidelity_score in pass range (>=0.78, <0.95). B-3 invariant live: voice_pin_sha == checkpoint_sha.
- PASS-HARDBLOCK-FAIL-CRITIC: state.json HARD_BLOCKED + 'failed_critic_after_R_attempts'. 4 DRAFTED_A + 4 CRITIC_FAIL + terminal HARD_BLOCKED. This is a VALID Phase 3 outcome — the whole point is to exercise the loop. Operator records which axes consistently failed (from audit log) as Phase 5 thesis candidates.
- PASS-HARDBLOCK-TRAINING-BLEED: HARD_BLOCKED + 'training_bleed'. V-2 live; caught a real bleed. Document the 12-gram in SUMMARY for Phase 6 digest.

FAIL outcomes (operator fixes infra + re-runs):
- HARD_BLOCKED 'mode_a_unavailable' → vLLM issue; go back to STEP 1.
- HARD_BLOCKED 'critic_blocked:anthropic_unavailable' → Anthropic auth/rate; check $ANTHROPIC_API_KEY.
- HARD_BLOCKED 'anchor_set_drift' → Plan 03-02 state out of sync; re-run curate-anchors.
- Python traceback outside HARD_BLOCKED taxonomy → integration gap; fix via revision pass BEFORE Phase 4.
</smoke_execution_plan>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end smoke on REAL vLLM + REAL Anthropic + REAL corpus (ch01_sc01)</name>
  <what-built>
    Plans 03-01 through 03-07 have all landed:
    - 03-01: kernel skeletons + V6 SHA pin + pin-voice CLI
    - 03-02: 20-30 anchors + voice_fidelity module + mode_thresholds voice_fidelity block
    - 03-03: vLLM systemd unit + VllmClient + boot_handshake SHA gate
    - 03-04: ModeADrafter (DRAFT-01 + DRAFT-02 + OBS-03)
    - 03-05: SceneCritic (CRIT-01 + CRIT-04)
    - 03-06: SceneLocalRegenerator (REGEN-01) — kernel only, Protocol-conformant
    - 03-07: book-pipeline draft CLI composition root + SceneStateMachine wiring + ch01_sc01 stub + mocked integration tests

    All mocked tests pass from 03-07. Task 1 (03-08) EXECUTES the real smoke as a forcing function: `book-pipeline draft ch01_sc01` against live vLLM (paul-v6-qwen3-32b-lora on :8002) + live Anthropic Opus 4.7 + live RAG indexes. Outcome is EITHER COMMITTED (drafts/ch01/ch01_sc01.md exists with frontmatter) OR HARD_BLOCKED (state.json shows terminal state with full history). Both are acceptable Phase 3 endings.

    Why human-verify: GPU resource contention with paul-thinkpiece-pipeline vllm-qwen122; Anthropic cost (~$0.6 per run); V-2 memorization gate may fire on real training data and HARD_BLOCK (that would be a V-2 success). Operator must acknowledge pre-flight checklist before Claude spends the budget.
  </what-built>
  <pre-flight-checklist>
    Before resuming Task 1, operator confirms:

    1. vLLM serving paul-v6-qwen3-32b-lora on 127.0.0.1:8002:
       - Option A (Plan 03-03 bootstrap): `uv run book-pipeline vllm-bootstrap --enable --start` (first time) OR `systemctl --user start vllm-paul-voice.service` (subsequent). Wait for `systemctl --user status vllm-paul-voice.service` to show "active (running)".
       - Option B (manual): operator-managed systemd or foreground vllm. As long as `curl -s http://127.0.0.1:8002/v1/models | jq '.data[].id'` returns "paul-voice", the pipeline is happy.
       - Verify SHA match: `uv run python -c "from book_pipeline.drafter.vllm_client import VllmClient; from book_pipeline.config.voice_pin import VoicePinConfig; from book_pipeline.book_specifics.vllm_endpoints import DEFAULT_BASE_URL; c = VllmClient(DEFAULT_BASE_URL); c.boot_handshake(VoicePinConfig().voice_pin); print('handshake OK')"` exits 0 (V-3 live on real weights).

    2. ANTHROPIC_API_KEY set in environment:
       - `echo $ANTHROPIC_API_KEY | head -c 10` shows "sk-ant-ap" (or similar prefix).

    3. GPU VRAM sufficient:
       - `nvidia-smi` shows >=80GB free on gpu 0 (Qwen3-32B bf16 ~65GB + BGE-M3 fp16 ~2GB + Anthropic is remote so 0 local).
       - If not, operator STOPS vllm-qwen122 on 8000 via `systemctl --user stop vllm-qwen122.service` (MEMORY.md rule) OR accepts sharing (Qwen-32B may OOM under contention — smoke is expected to succeed with dedicated GPU).

    4. RAG indexes populated:
       - `ls indexes/` shows all 5 axis directories non-empty + voice_anchors/.
       - Baseline ingestion_run_id: `cat indexes/resolved_model_revision.json 2>/dev/null | jq .sha` == "5617a9f61b028005a4858fdac845db406aefb181" (Phase 2 baseline).

    5. voice_pin.yaml has real V6 SHA (Plan 03-01 Task 3 already executed):
       - `grep -v TBD-phase3 config/voice_pin.yaml | grep checkpoint_sha` shows 64-hex.

    6. anchor_set_v1.yaml + embeddings.parquet exist (Plan 03-02 Task 2 already executed):
       - `ls config/voice_anchors/anchor_set_v1.yaml indexes/voice_anchors/embeddings.parquet`.

    7. Anthropic budget OK for ~$0.6 (1 scene × up to 8 LLM calls).
  </pre-flight-checklist>
  <action>
    This is a human-verify checkpoint — the "action" is the operator-executed smoke defined in <how-to-verify>. Claude does NOT run the real smoke unilaterally; the operator must acknowledge pre-flight + resume the task. After operator completes STEP 1-6 of <how-to-verify> and signals per <resume-signal>, Claude writes 03-08-SUMMARY.md capturing the outcome (COMMITTED or HARD_BLOCKED) and any deferred Phase 4 issues.
  </action>
  <how-to-verify>
    STEP 1 — Pre-flight (operator runs before authorizing Task 1 resume):
    ```
    cd /home/admin/Source/our-lady-book-pipeline
    # vLLM health + handshake
    curl -s http://127.0.0.1:8002/v1/models | jq '.data[].id'
    # Expected: "paul-voice"
    uv run python -c "from book_pipeline.drafter.vllm_client import VllmClient; from book_pipeline.config.voice_pin import VoicePinConfig; from book_pipeline.book_specifics.vllm_endpoints import DEFAULT_BASE_URL; c = VllmClient(DEFAULT_BASE_URL); c.boot_handshake(VoicePinConfig().voice_pin); print('handshake OK')"
    # Expected: 'handshake OK' (V-3 live passes on real weights)

    # Anthropic key
    test -n "$ANTHROPIC_API_KEY" && echo "key set" || echo "MISSING KEY"

    # GPU
    nvidia-smi | head -15

    # RAG indexes
    ls indexes/
    # Expected: historical/ metaphysics/ entity_state/ arc_position/ negative_constraint/ voice_anchors/ resolved_model_revision.json
    ```

    STEP 2 — Dry-run (zero LLM cost, confirms wiring):
    ```
    uv run book-pipeline draft ch01_sc01 --dry-run
    # Expected: prints pack fingerprint + num_hits per axis + num_conflicts; exits 0.
    ```

    STEP 3 — REAL smoke (the actual spend):
    ```
    PYTHONUNBUFFERED=1 uv run book-pipeline draft ch01_sc01 --max-regen 3 2>&1 | tee runs/smoke_ch01_sc01_$(date +%Y%m%d_%H%M%S).log
    # Wall time: 3-10 minutes. Budget: ~$0.6.
    ```

    STEP 4 — Inspect outcome:
    ```
    # Terminal state
    cat drafts/scene_buffer/ch01/ch01_sc01.state.json | jq '.state,.attempts,.blockers,.history[-1]'

    # If COMMITTED (happy path):
    cat drafts/ch01/ch01_sc01.md
    # Verify: YAML frontmatter has all 9 keys, voice_pin_sha == checkpoint_sha (B-3 invariant live),
    # scene_text is ~1000 words, mode:A, voice_fidelity_score in [0.78, 0.95] band

    # If HARD_BLOCKED (also acceptable):
    jq '.blockers, .history' drafts/scene_buffer/ch01/ch01_sc01.state.json
    # Verify: blockers contains 'failed_critic_after_R_attempts' OR 'training_bleed' OR 'mode_a_unavailable'

    # Events trail
    tail -30 runs/events.jsonl | jq -c '[.role, .caller_context.scene_id, .caller_context.attempt_number, .mode, (.checkpoint_sha // "")[:8], .rubric_version // "", (.extra.status // "ok")]'
    # Expected: mix of "context_pack_bundler", "retriever" (x5), "drafter", "critic", optionally "regenerator" + "critic" pairs, "cli_draft" at the end

    # Critic audit records (if reached critic at least once)
    ls runs/critic_audit/ch01_sc01_*.json
    jq '.rubric_version, .model_id, .caching_cache_control_applied, .cached_input_tokens, .parsed_critic_response' runs/critic_audit/ch01_sc01_01_*.json
    # Expected: "v1", "claude-opus-4-7", true, <int>, <null or CriticResponse dict>
    # W-7: failure-path audits ALSO land (parsed_critic_response=null; raw_anthropic_response={"error":..., "attempts_made":5})
    ```

    STEP 5 — Acceptable outcomes (any one = Phase 3 PASS):
    - PASS-COMMIT: drafts/ch01/ch01_sc01.md with all 9 frontmatter keys, voice_pin_sha == checkpoint_sha (B-3 live), scene_text matches voice (subjective — operator reads + judges).
    - PASS-HARDBLOCK-FAIL-CRITIC: state.json HARD_BLOCKED + 'failed_critic_after_R_attempts'. Expected behavior for a V6 model that can't pass the critic on 4 tries of a complex historical-fiction beat. Operator records which axes consistently failed (from audit log) — Phase 5 thesis candidates.
    - PASS-HARDBLOCK-TRAINING-BLEED: HARD_BLOCKED + 'training_bleed'. V-2 live; caught a real bleed. Document the 12-gram in SUMMARY for Phase 6 digest.

    FAIL outcomes (operator fixes + retries):
    - HARD_BLOCKED 'mode_a_unavailable' → vLLM issue; go back to STEP 1.
    - HARD_BLOCKED 'critic_blocked:anthropic_unavailable' → Anthropic auth/rate.
    - HARD_BLOCKED 'anchor_set_drift' → Plan 03-02 state out of sync; re-run curate-anchors.
    - Python traceback outside the HARD_BLOCKED taxonomy → integration gap; operator signals "failed: <describe>" and Claude fixes via a gap-closure plan BEFORE Phase 4.

    STEP 6 — Commit outcome:
    - If PASS-COMMIT: `git add drafts/ch01/ch01_sc01.md` + commit with message noting the scene committed.
    - If PASS-HARDBLOCK: `git add -f drafts/scene_buffer/ch01/ch01_sc01.state.json` (force add overrides the .gitignore for this one file) + commit documenting the blocker.
    - Either way: `git add runs/smoke_ch01_sc01_*.log` + commit the smoke log for the Phase 3 SUMMARY's record.
  </how-to-verify>
  <resume-signal>
    Reply "approved: committed" (ch01_sc01 landed in drafts/ch01/ch01_sc01.md with expected frontmatter + readable prose).
    OR "approved: hardblocked <reason>" where reason is the specific blocker from state.json (failed_critic_after_R_attempts | training_bleed | anchor_set_drift). This also passes Phase 3 — the loop exercised the full state machine.
    OR "blocked: <describe infra/environment gap>" for operator-fixable pre-flight gaps (wait for operator to fix + retry).
    OR "failed: <describe code gap>" for unhandled exception — Claude opens a gap-closure plan BEFORE Phase 4.

    On approval, Claude writes `.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-08-SUMMARY.md` documenting the outcome + closing Phase 3.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → pre-flight checklist | Operator-acknowledged; Claude cannot verify environment pre-conditions without running the real smoke (which is what's gated). |
| Task 1 smoke → real infra | Operator controls vLLM + Anthropic credentials + GPU allocation; pipeline cannot; human-verify checkpoint gates this. |
| runs/smoke_ch01_sc01_*.log → repo | Tee'd smoke log committed for audit trail; operator greps for "sk-ant-" before commit to prevent key leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Denial of Service | Task 1 real smoke blows past the $0.6 budget due to runaway regen | mitigate | --max-regen R=3 is the hard cap (R=3 regens = 4 total attempts × 2 LLM calls per attempt = 8 max). Phase 5 REGEN-02 will add $ per-scene cap. |
| T-03-08-02 | Information Disclosure | Task 1 smoke log contains ANTHROPIC_API_KEY (via tenacity error message or traceback) | mitigate | anthropic SDK redacts auth headers in 4xx bodies; tenacity's reraise=True preserves the original exception which does not leak the key. STEP 6: operator pre-commit greps for "sk-ant-" in the committed smoke log before `git add`. |
| T-03-08-03 | Denial of Service | Task 1 smoke fails and operator retries; state.json accumulates history from both runs | mitigate | _load_or_init_record (Plan 03-07) preserves history across reinvocations (feature, not bug — state machine is resumable). Task 1 STEP 4 acceptance: review history + confirm the RECENT run's terminal transition is what operator expected; earlier-run history is audit trail. |
| T-03-08-04 | Tampering | vLLM serves a DIFFERENT adapter than voice_pin.yaml pins (e.g., adapter path changed between pin and serve) | mitigate | boot_handshake in Plan 03-03 verifies SHA match; pre-flight STEP 1 runs the handshake manually; mismatch → HARD_BLOCKED('checkpoint_sha_mismatch') within the smoke. Acceptable fail; operator fixes + re-pins. |
| T-03-08-05 | Repudiation | Real smoke produces a terminal state but no events | mitigate | Every stage (Plans 03-04/05/06/07) emits Events; Task 1 STEP 4 counts events in runs/events.jsonl. The draft loop's cli_draft wrap-up event is the end-of-run signal. |
| T-03-08-06 | Information Disclosure | Scene text committed to drafts/ch01/ may be accidentally pushed to public git | accept | Repo is private; single-user pipeline. Same trust boundary as canon/ (which IS the book). |
</threat_model>

<verification>
1. Operator executes pre-flight STEP 1 cleanly (vLLM healthy, ANTHROPIC_API_KEY set, GPU VRAM >=80GB free, RAG indexes populated, voice_pin real SHA, anchor set committed).
2. Dry-run STEP 2 exits 0 (wiring sanity — no LLM calls).
3. STEP 3 real smoke runs to completion within ~10 min + ~$0.6 spend.
4. STEP 4 inspection confirms one of the three PASS outcomes (COMMIT | HARDBLOCK-FAIL-CRITIC | HARDBLOCK-TRAINING-BLEED).
5. STEP 5: operator judges PASS or FAIL; FAIL triggers a revision pass.
6. STEP 6: operator commits artifacts (scene md OR state.json via `git add -f`) + smoke log (post-grep for "sk-ant-").
7. On approval, Claude writes 03-08-SUMMARY.md closing Phase 3.
</verification>

<success_criteria>
- Phase 3 real-world forcing function executed: end-to-end scene loop against real vLLM + real Anthropic + real RAG + real voice anchors.
- EITHER COMMITTED or HARD_BLOCKED terminal state — both pass if they land cleanly within taxonomy.
- B-3 invariant validated on real output (voice_pin_sha == checkpoint_sha in written frontmatter).
- CRIT-04 validated: audit records on disk with real Opus payload + cache_control hit on attempt 2+.
- OBS-03 validated: voice_fidelity_score attached to real drafter Event.
- V-2 memorization gate exercised against real training corpus + real V6 output.
- V-3 SHA verify exercised on real LoRA adapter weights.
- Phase 3 DONE. 6 requirements complete: DRAFT-01, DRAFT-02, CRIT-01, CRIT-04, REGEN-01, OBS-03.
</success_criteria>

<output>
After operator approval (per <resume-signal>), Claude writes `.planning/phases/03-mode-a-drafter-scene-critic-basic-regen/03-08-SUMMARY.md` capturing:
- Final terminal state of ch01_sc01 smoke (COMMITTED or HARD_BLOCKED with specific blocker).
- Token counts + wall time + $ spend of the real smoke run (extracted from tee'd smoke log + runs/events.jsonl).
- Which CriticIssues fired (if any), which axes failed, which severities — Phase 5 thesis candidates.
- voice_fidelity_score observed on the final DraftResponse (compare to Plan 03-02 threshold band: pass >=0.78, flag 0.75-0.78, fail <0.75, memorization_flag >=0.95).
- B-3 invariant check on real output: frontmatter.voice_pin_sha == frontmatter.checkpoint_sha (both in drafts/ch01/ch01_sc01.md).
- CRIT-04 audit-record integrity check: N audit JSONs on disk matching the N critic calls from events.jsonl; each has the 11 keys from <audit_record_shape>; failure-path audits (W-7) have parsed_critic_response=null + raw_anthropic_response.error populated.
- Any deferred issues surfaced during the smoke — integration gaps Phase 4 inherits.
- Plan closure: Phase 3 DONE. 6 REQs complete:
  - DRAFT-01 (Plan 03-01 pin + 03-03 vllm + 03-04 drafter)
  - DRAFT-02 (Plan 03-04 sampling profiles)
  - CRIT-01 + CRIT-04 (Plan 03-05)
  - REGEN-01 (Plan 03-06)
  - OBS-03 (Plans 03-01 + 03-02 + 03-04)
- Pattern catalog for Phase 4: (a) CLI composition root pattern from cli/draft.py (Plan 03-07), (b) SceneStateMachine persistence + atomic tmp+rename, (c) commit-scene YAML frontmatter shape is Phase 4 ChapterAssembler input contract (B-3 invariant: voice_pin_sha == checkpoint_sha), (d) Event role taxonomy now established: retriever, context_pack_bundler, drafter, critic, regenerator, anchor_curator, voice_pin, vllm_bootstrap, vllm_boot_handshake, cli_draft.
- Handoff to Phase 5: HARD_BLOCKED('failed_critic_after_R_attempts') is the DOCUMENTED edge REGEN-03 re-routes to Mode-B; drafts/scene_buffer/{chapter}/{scene_id}.state.json is the source of truth for re-routing.
</output>
</content>
