# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
venv_*/
.venv/
env/

# Runtime artifacts
runs/*.jsonl
runs/ablations/
runs/state.json
runs/critic_audit/
indexes/*.db
indexes/*.sqlite
indexes/*.lance/
indexes/*.parquet
indexes/**/*.parquet
indexes/mtime_index.json
indexes/resolved_model_revision.json
drafts/in_flight/
drafts/retrieval_conflicts/
# Plan 03-07: per-scene SceneStateRecord buffer — transient, rebuildable
# from runs/events.jsonl. Committed scenes land in drafts/ch{NN}/{scene_id}.md
# (which ARE tracked).
drafts/scene_buffer/
# Plan 05-03: TelegramAlerter CooldownCache state — transient, rebuildable
# from runs/events.jsonl. Survives process restart for 1h TTL dedup but is
# advisory state, not source of truth.
runs/alert_cooldowns.json

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Secrets
.env
.env.*
!.env.example
config/secrets.yaml

# openclaw local state
.openclaw/runs/
.openclaw/state/

# Big or ephemeral
*.log
*.tmp
*.pyc
